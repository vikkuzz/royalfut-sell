import { GeneralHeader } from "@royalfut/components";

import type { FNCN } from "@royalfut/interfaces";

const Header: FNCN = ({ className }) => {
    return (
        <GeneralHeader className={className}>
            <></>
        </GeneralHeader>
    );
};

export default Header;
