import type { FNCN } from "@royalfut/interfaces";
import { cn } from "@royalfut/utils";

const Swiper: FNCN = ({ className }) => {
    return <div className={cn("flex", className)}>Swiper</div>;
};

export default Swiper;
